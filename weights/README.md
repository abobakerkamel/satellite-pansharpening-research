# Weights

`train_normalization_stats.json` is included here.

The small PanTiny checkpoint is included under `models/pantiny/weights/`.

The large HAT checkpoint should be uploaded through **GitHub Releases**, not the normal browser code upload.
