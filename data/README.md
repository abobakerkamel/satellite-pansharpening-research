# Data

Raw satellite imagery and generated training data are intentionally not included.

Expected project inputs include aligned six-band MS + one-band PAN GeoTIFFs and Wald train/validation/test patches.

Only publish the imagery if redistribution is permitted.
